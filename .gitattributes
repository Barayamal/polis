# This helps prevent conflicts (well, at last when GitHub implements it...)
# See: https://github.com/isaacs/github/issues/487
CHANGELOG.md merge=union

# Patch payloads intentionally preserve upstream source whitespace. Treat their
# encoded additions as data rather than repository whitespace errors.
*.patch -whitespace
